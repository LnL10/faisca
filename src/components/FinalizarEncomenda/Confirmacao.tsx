export default function Confirmacao(props:any){

    return(
        <div>
        <div>CONFIRMAÇAO</div>
        <button className="botao" onClick={props.back}>Voltar</button>
        </div>

    )
}